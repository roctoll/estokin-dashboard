<h1>Feature list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Field</th>
            <th>Value</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('feature_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.field }}</td>
            <td>{{ entity.value }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('feature_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('feature_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('feature_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
